# CRM Celery Setup

### Requirements
- Redis installed and running on `localhost:6379`
- Celery
- Django Celery Beat

### Setup Steps
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
